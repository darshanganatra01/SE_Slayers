# SE_Project 

Instructions to run the application :

After extracting the folder :

1. cd into the root folder 'SE_Slayers-master'

2. run command "npm i"

3. run command 'npm run dev'

4. open the localhost url.
